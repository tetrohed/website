import { ViewComponent, View } from '@arminjazi/dom';

export const ComingSoon: ViewComponent = (): View => {
  return (
    <div data-testid="ComingSoon">
      <p> Coming Soon!</p>
    </div>
  );
};
