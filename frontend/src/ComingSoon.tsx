import { NodeComponent, Node } from '@arminjazi/dom';

export const ComingSoon: NodeComponent = (): Node => {
  return (
    <div data-testid='ComingSoon'>
      <p> Coming Soon!</p>
    </div>
  );
};
